<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_peach" /> <!-- Background color of the card -->
    <corners android:radius="16dp" /> <!-- Rounded corner radius -->
    <padding android:left="16dp" android:right="16dp" android:top="16dp" android:bottom="16dp" />
    <stroke android:width="1dp" android:color="@color/purple" /> <!-- Optional border -->
</shape>
